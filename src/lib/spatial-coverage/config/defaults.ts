/**
 * Default configuration for spatial-coverage microservice
 */

/** Default voxel size in meters (5cm for survey-grade precision) */
export const DEFAULT_VOXEL_SIZE = 0.05;

/** Coverage percentage threshold for auto-complete */
export const AUTO_COMPLETE_THRESHOLD = 98;

/** Minimum points required for a valid boundary */
export const MIN_BOUNDARY_POINTS = 3;
